name: agri_guide_app
description: A new Flutter project

publish_to: 'none' # Remove this line if you plan to publish your package

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^1.0.0  # Ensure this line is included

dev_dependencies:
  flutter_test:
    sdk: flutter

# Ensure this path matches the location of your PNG file
assets:
  - assets/images/new_image.png